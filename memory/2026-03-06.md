## 2026-03-06

### 今日记录（凌晨）

#### 主线任务：MIGRATION_EXEC_PLAN 执行
- n8n 本地环境已跑通（版本 2.9.4，Web 端口 5678 可访问）。
- `rss_collect_v1` 工作流已导入并激活，触发频率为每2小时。
- 已产出数据文件：
  - `openclaw-monitor/data/items_20260305.jsonl`
  - `openclaw-monitor/data/items_20260306.jsonl`
- 已完成去重与日志落地：
  - `openclaw-monitor/data/seen_ids.json`
  - `openclaw-monitor/data/run_log.json`
  - `openclaw-monitor/data/run_log_latest.json`
- 已发送一次 Telegram 采集验证消息（messageId: 609）。

#### 关键问题与修复
- 问题1：n8n import 初始失败（workflow id 约束）
  - 修复：补齐 `id` 字段后重导入。
- 问题2：Code 节点 `require('crypto')` 被禁用
  - 修复：改为无 crypto 的 id 生成策略（url/title+time）。
- 问题3：RSS 源出现 429 限流（hnrss）
  - 修复：切换到 `https://github.blog/feed/` 作为稳定源。
- 问题4：Write Items 节点写盘不稳定
  - 处理：先通过执行结果回写保证产物，后续继续修节点直写。

#### 第二实例（小龙虾2号）
- 方案采用：单机双实例（端口隔离）。
- 已创建：`C:\Users\17763\.openclaw2` 独立目录与工作区。
- 已完成：
  - 第二实例配置复制并隔离（端口 18790、Telegram 关闭）。
  - 第二实例基础文件初始化（AGENTS/SOUL/USER/MEMORY/IDENTITY）。
  - 第二实例网关启动（`http://127.0.0.1:18790/`）。
- 已处理认证问题：补齐 `auth-profiles.json` 到第二实例 agent 目录。

#### 执行纪律复盘
- 发生过多次超时与“准备态空转”，已记录错误并执行纠偏。
- 后续规则继续执行：
  - 只报可验证产物；
  - 子任务5分钟超时未完成即判失败；
  - 主线优先，学习/巡检不抢主线。

#### 下一步
1. 修复 n8n `Write Items JSONL` 节点直写稳定性（不用回写兜底）。
2. 采集链路补自动触发证据（下一轮 schedule 的新增 run 记录）。
3. 将 MIGRATION 执行日志与 run_log 合并成单一验收看板。
