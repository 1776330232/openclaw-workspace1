## 2026-03-11

### 00:00-01:22：今晚补充内容

**opik-openclaw 安装**
- 已安装 v0.2.8，手动跑 npm install 解决依赖问题
- 配置：apiKey=B58aMvsQx3i9XpYGanVv583YQ，workspace=hang056917，project=openclaw-xiapi
- 问题：Comet 面板显示 "This is a private project"，原因未明（workspace 名可能不对，或 trace 未发到）
- 待做：task-202603110054（验证 trace 到达 + 修 workspace）

**双回流通知修复**
- 发现问题：researcher 通过 message 工具发到 Discord 的 bot 消息不会触发 main agent inbound，主 agent 无法知道 researcher 完成
- 修复：researcher AGENTS.md 新增强制回执规则——双回流完成后必须 sessions_send 回主 agent session

**Control UI 红字可编辑**
- 修改 index-B8TWcHZF.js line 8100：${e.key} → ${e.label??e.key}
- 效果：在标签输入框填名字后，红字显示标签而非 session key
- Hang 已填入三个频道标签：虾皮主频道/研究生/书签推送

**Hang 信息补全（落进 USER.md）**
- 全职 All in，每天 10 小时起步
- 无编程基础，靠 AI 工具干活
- 变现优先级：内容 > 服务（OpenClaw 配置） > 其他
- 最强比较优势：OpenClaw 配置/使用
- 内容主战场：小红书/X中文区/B站
- 资金：可以花钱

**本轮系统状态**
- opik 插件 + 114k/200k tokens → 回复明显变慢
- 需开新对话

**待做事项**
- opik workspace 名验证（task-202603110054）
- 虾皮号内容方向调整（OpenClaw 相关选题）
- 下一步行动方向讨论（内容 vs 服务优先级）

### 01:32-03:05：多 agent 委托-监督系统搭建

**架构决策**
- 用 coordinator-specialist 模式，orchestrator 永不亲自执行
- supervisor 用检查点式审计（不是流式监控，OpenClaw 无 pub/sub）
- sessions_spawn 是 orchestrator 专属，小弟不能 spawn 孙子
- 小弟使用 subagent runtime，不用 ACP（ACP 是给外部 coding agent 的）

**新建文件**
- `workspace/agents/executor-rules.md`
- `workspace/agents/browser-operator-rules.md`
- `workspace/agents/content-creator-rules.md`
- `workspace/status.md`（共享状态文件）
- `workspace/docs/MULTI-AGENT-SYSTEM.md`（运行手册）

**规则更新**
- AGENTS.md：新增多 agent 调度路由表 + sessions_spawn 架构铁律
- workspace-researcher/AGENTS.md：新增 subagent 模式补充规则
- content-creator-rules.md：新增强制落盘规则

**验证结果**（Supervisor 全程审计）
- Phase 1-6 全部通过，Supervisor 最终签收 7 项 ✅
- 3 agent 并行协作测试通过（researcher + executor + content-creator）
- 检查点式审计机制有效（RUNNING 超 30 分钟可检测）

**遗留**
- camofox 未安装（需 camoufox fetch）
- persona-xiapi.md 不存在（当前用 SOUL.md 替代）

**技能安装测试**
- tavily：✅ 完全可用（tavily-python v0.7.23，API key 有效）
- video-watcher：✅ 可用（yt-dlp 2026.03.03 已装，字幕抓取正常）
- notion-api：⚠️ Token 有效但 Integration 未授权到任何页面，待用时再授权

**今日经验教训**
- ERR-20260311-001：架构类任务跳过 research 直接动手，已记录
- GPT-5.4 作为 model 可直接用于 subagent spawn，不需要 acpx

### 03:50-03:59：MiniMax 可用性复核

- Hang 质疑旧结论“MiniMax 2.5 fallback 有 bug，实际不可用”可能是错的，要求先测试
- 已派 executor 做最小复现；阶段结果：**MiniMax 模型本身已证明确实能跑**
- 当前已足够支持业务判断：MiniMax **可以拉进现有架构**；不再把“实际不可用”当定论
- fallback 机制是否有独立 bug 暂未彻底查死，但 Hang 决定到此收口，不继续深挖
- 需修正长期记忆中的旧表述，避免把“曾经的怀疑”写成永久事实

### 04:08-04:26：runtime-ops 落地与验收

- researcher 调研结论：运行层缺口真实存在，但**不值得补独立 ops agent**；更合理的是做 `runtime-ops` 能力层
- orchestrator 拍板：不新增角色，改为 orchestrator + supervisor + heartbeat/cron 分摊运行层职责
- architect 已产出蓝图：超时判断基准、失败处理路径、任务完成收口、supervisor 审计边界、heartbeat 检查项
- executor 已完成第一轮施工：`AGENTS.md` + `HEARTBEAT.md`
- supervisor 第一次验收抓到硬伤：`status.md` 是悬空引用，不应继续存在
- 已决定不恢复 `status.md`，统一收口到 `tasks.json` / 每日日志 / `MEMORY.md` / `SESSION-STATE*.md`
- executor 第二轮修掉一处收口引用后，supervisor 复验又抓到 AGENTS.md 另一处残留 `status.md`
- 最后一刀由我直接修掉：从 orchestrator 的“保留直接操作”列表中删除 `status.md`

### 03:27-03:47：architect 补齐 + 架构类工作流澄清

- Hang 决定：researcher 模型分工改为 **简单任务 Sonnet / 深度调研 Opus**
- 已完成：新增 `workspace/agents/architect-rules.md`，并更新 `docs/MULTI-AGENT-SYSTEM.md`、`AGENTS.md`、`MEMORY.md`
- architect 已拉起并待命；当前作为可调用的架构设计角色存在
- 关键澄清：**架构类问题不能因为只是“判断缺什么”就跳过工作流**
- 新共识流程：**orchestrator 先给方向假设 → researcher 先搜现成方案 → orchestrator 先判断是否成立/值不值得做 → architect 针对选中方向出蓝图 → orchestrator 再拍板是否落地**
- 角色边界再确认：是否值得现在做、优先级排第几，是 orchestrator 判断；architect 负责把已选方向设计明白，不负责战略取舍
- Hang 认可：以后系统题先给方向，如果值得动，就先搜再干

### 03:00-03:14：模型安排讨论

**最终模型分工共识**
- orchestrator（主会话）：Sonnet
- researcher：Opus（深度调研），Sonnet（轻量查询）
- executor：GPT-5.4（代码/文件操作，不耗 Anthropic 额度）
- content-creator / browser-operator / supervisor：Sonnet
- 一次性架构设计：按需派 Opus subagent

**最烧 token 的角色**
- 数量最多：executor（读大文件，21-25k input/次），但走 OpenAI 额度
- 成本最高：researcher（Opus 单价 × web fetch 内容叠加）

**MiniMax**：旧结论需修正。重新测试后已确认模型本身能跑、能拉进当前架构；此前“fallback bug 未修，实际不可用”不能再当定论

**Discord thread**：`多Agent系统搭建项目`（1480991575617175714）保存完整审计轨迹

### 04:30-05:07：MiniMax 粗筛链落地 + 首次真实测试进展

- 新任务：并行评估两条扩编线——MiniMax 接入方案 + 下一批角色候选；最终拍板为：**MiniMax 先做 researcher 前置粗筛层，下一批角色候选优先级最高的是 Editor，但先不落地**
- researcher 调研结论：MiniMax 最适合作为**便宜的资料预处理层**，不适合直接接管 researcher 整体；已明确规则：**MiniMax 只做资料预处理，不做最终判断，不直接面向 Hang 输出**
- architect 已产出 `MiniMax → researcher 前置粗筛层` 蓝图：MiniMax = 有序原材料仓库；Researcher = 最终分析师；两者之间用结构化 schema + 三道验证闸隔离幻觉
- 已完成 Phase 1 落地：新建模板文件
  - `workspace/agents/minimax-prescreen-brief.md`
  - `workspace/agents/researcher-handoff-protocol.md`
- 为首次真实测试选题：`task-202603100searxng` —— **评估 SearxNG 替换 Perplexica 接入 OpenClaw 生态是否值得做**
- A 组基线已完成：researcher 直接调研同题，结论是——**SearxNG 作为 Brave 备份搜索源有价值，但不是当前优先级；“替换 Perplexica”本身是伪命题，因为当前并未使用 Perplexica**
- B 组首次失败不是模型本身问题，而是接入白名单问题：`sessions_spawn` 报 `model not allowed`，根因是 `minimax/MiniMax-M2.5` **没进 `agents.defaults.models` 允许列表**
- 已用 `gateway config.patch` 修复：把 `minimax/MiniMax-M2.5` 加入 `agents.defaults.models`，并重启成功；说明 **MiniMax provider 其实早已配好，卡点只在 allowlist**
- 重跑 B 组后，MiniMax 子会话已成功起跑并真正在用 `MiniMax-M2.5` 调 `web_search`；说明**模型入口已打通**
- 当前新判断：B 组现在卡的不是接入，而是**第一次真实任务在工具调用后的续跑稳定性**；最可能需要缩小任务规模（减少搜索维度/来源数）再重试
- 已拍板：这次 B 组**直接判失败**，不再继续等；定性为“MiniMax 接入口成功，但首个真实粗筛任务执行失败”
- 后续正确路径：把这次记为失败样本，换更小任务规模重试，而不是继续拿大任务硬压
- 新规则已写入 `AGENTS.md`：**同一任务、同一内容的 completion receipt 只确认一次；后续重复回执直接静默**，避免 researcher 双回流重复回执导致主会话复读

### 05:15：MiniMax 小样本粗筛测试跑通

- 新测试题从大题缩到窄题：**“SearxNG 能不能作为 OpenClaw 的 Brave 备份搜索源？”**
- 约束：最多 2 次 web_search，不扩展到 Perplexica/整套架构讨论，只做 researcher 前置资料预处理
- **结果：MiniMax 小样本任务已顺利跑完**，输出了结构化粗筛结果，说明它在“小任务资料预处理层”上至少能稳定完成一次
- 粗筛结论：**技术可行，但不是即插即用**；SearxNG 有 JSON API、社区已有相关 PR/feature request，适合作为备份搜索源候选，但仍需适配层
- 仍未解决的点：PR 是否已合并、搜索质量/速度相对 Brave 的实测、官方 roadmap 优先级
- 当前判断更新：**MiniMax 入口已通 + 小样本粗筛可跑**；下一步不是直接扩容，而是再补 1-2 个小样本，确认不是偶然成功
- 第二个小样本（轻比较型）也已跑通，题目是：**“Tavily 和 Brave Search，更适合做 OpenClaw 的研究型搜索源候选的是谁？”**
- MiniMax 本次给出的粗筛判断：**Tavily 更偏研究型搜索源候选**，因为它本身就是为 AI agents / RAG 设计，带 research 端点；Brave 更像独立搜索底座，适合自己处理内容的场景
- 当前阶段性判断升级为：**MiniMax 不只是能跑单一道题，而是已经连续跑通 2 个小样本题型（事实核验型 + 轻比较型）**
- 但仍不宜直接扩容到大题；下一步更合理的是再测 1 个不同类型小题，过了再决定是否正式挂到 researcher 前面做粗筛层
- 第三个小样本（风险识别型）也已跑通，题目是：**“如果把 SearxNG 接进 OpenClaw，当 Brave 的备份搜索源，最可能先踩到的 3 个坑是什么？”**
- MiniMax 本次给出的前三风险：**公开实例 rate limit 很严、与现有服务可能端口冲突、SearxNG API 结果条数控制能力弱**
- 当前阶段性结论可以收口为：**MiniMax 已连续跑通 3 个不同类型小样本（事实核验 / 轻比较 / 风险识别），足以证明它适合做 researcher 前面的便宜粗筛层候选**
- 但边界也应一并钉死：**只让它做小范围资料预处理，不让它直接做大题、不让它给最终判断**

### 05:23：MiniMax → researcher handoff 链首测完成

- 已完成一次真实 handoff 测试：MiniMax 先产出 `handoffs/tavily_handoff_20260311.md`，researcher 再基于该包做轻量验证
- researcher 裁定：**handoff 质量 = 部分可用**；不是废纸，能省前置整理成本，但不能照单全收
- 这条链的价值被证实：researcher 不必重做“工具是什么/该从哪几条线深挖”的基础整理，估计可省 **30-40%** 前置整理成本
- 暴露出的两个关键问题：
  1. **数字/价格类信息容易误导**：如 Tavily 免费 tier 被漏掉，说明 MiniMax 给数值类信息时必须带来源与置信度
  2. **框架类判断容易带偏方向**：如把 Tavily JS 渲染和 Camoufox 当竞品比较，researcher 判断这其实是错误问题，正确问题应是两者能否互补
- researcher 给出的协议修正建议：
  - 所有数字字段必须带 `[source]` + `[confidence]`
  - 架构/关系判断（竞品/互补）必须打 `[needs_validation]`
  - 涉及产品核心差异时，粗筛阶段必须强制读官方 API 文档的 response schema
- 当前判断升级为：**MiniMax → researcher 交接链可以保留，但协议必须收紧；它适合作为“参考地图”，不适合作为“可直接照单执行的结论包”**
- 已直接收紧两份模板：`agents/minimax-prescreen-brief.md` + `agents/researcher-handoff-protocol.md`
- 新增硬约束：**数字类信息必须带 `source + confidence`；框架类判断必须进 `framework_judgments` 且打 `needs_validation=true`；涉及产品核心差异时，粗筛阶段必须补读官方 API 文档 / response schema**
- researcher 侧同步补了验收规则：如果 handoff 缺上述标记，默认视为质量缺口，必要时直接弃用或强制补搜/补读官方文档

### 05:33：按新协议回归测试完整链，结果=机制通过、事实完整性未过

- 已无人盯梢跑完一整条链：MiniMax 按新协议产出 `handoffs/tavily-answer-field-handoff-20260311.md` → researcher 接手验证 → orchestrator 收口
- **好消息**：整条链确实能自己跑完，不需要 Hang 盯着每一步；`tasks.json`、`SESSION-STATE.md`、模板、handoff、researcher 回执都按流程闭环
- **协议机制层面通过**：数字类字段已带 `source + confidence`；框架类判断已带 `needs_validation`；官方 API 文档/schema 覆盖也比上一轮明显更好
- **但事实完整性层面没过**：MiniMax 在判断 “Tavily answer 字段是否构成相对 Brave 的核心差异” 时，漏掉了 **Brave Summarizer API**
- 这导致结论表述需要改写：不是“Brave 没有 answer 能力”，而是“Brave 有类似能力，但路径更重（两步调用 + Pro AI 门槛）；Tavily 的优势更像集成体验和门槛，不是能力有无”
- 当前结论：**这条链已经证明能自主跑完整闭环，但还不能保证首次 handoff 的事实完整性**；更像“能自动跑流程”，还没到“自动跑出来就能放心信”的阶段
- 下一步正确动作不是重做整套协议，而是补一个 **patch 型 handoff 修正包**，专门把 Brave Summarizer API 补进对比，再看第二轮回归是否过

### 05:48-05:49：协作交互规则再收紧

- Hang 明确要求：继续优化交互，重点是**不用他盯着也能跑完整条链**
- 已把两层规则写进 `AGENTS.md`：
  1. **自主继续 / 自主停止规则**：同链小修正自己补，方向升级才停下来问
  2. **3 行状态回报格式 + Hang 指令短语层**：默认把“继续 / 直接干 / 别让我盯 / 收掉 / 先别扩 / 你自己做”等短语解释写死
- 当前目标：减少 Hang 充当监工的频率，让虾皮默认跑到自然收口点再回报

### 05:52-05:54：MiniMax→researcher 文章初稿产出

- 已生成文章初稿：`content/minimax-to-researcher-article-20260311.md`
- 初稿主题：把今天 MiniMax 接入 researcher 前置粗筛链的实战过程写成一篇文章，重点写清：为什么只做粗筛、不做接管；怎么从大题失败缩到小样本；handoff 怎么暴露协议问题；为什么最后定性是“能用，但得拴绳”
- 已做一轮主会话质检并直接修稿：删掉了初稿里不准确的虚构细节（如不存在的字段/错误的 Brave Summarizer 使用表述），改成基于今天真实过程的版本
- Hang 随后明确指出：旧稿“解决面不大、内容太长”；已按这个判断重写成更通用的版本，核心角度抬高为：**便宜模型怎么被驯化进可靠工作流**，不再写成单次 MiniMax 接入复盘
- 06:04 后继续压成起号阶段更狠的短版：`content/minimax-to-researcher-article-20260311-short.md`，主标题改为 **《便宜模型最大的问题，不是笨，是你总让它越权》**，只保留最能打的矛盾、两个核心坑和一句收口结论
- 06:06 再砍成超短打版：`content/minimax-to-researcher-article-20260311-micro.md`，按“起号脑”处理，只保留一个冲突、两个坑和一句结论，目标是一口气读完
- 06:09 按 Hang 要求再修一轮：把 `researcher` 改成中文“研究员”，并继续往起号方向收，减少术语门槛
- 06:12 标题定稿为 **《便宜模型老是翻车，不是因为笨，是因为你总让它越权》**，并同步改到 micro 短版正文
- 06:15 Hang 明确纠正：这版内容“体感还行但缺点感觉”，本质上是**没调研过，只靠经验硬写**；以后做起号内容，不能只靠内部判断和写作手感，仍要先补一轮外部样本/成熟写法研究，再下笔
- 06:19-06:24 已补一轮 X 热帖写法研究，结论：这条内容更适合**单帖**，且应从用户行为/常见错误切入，不要从结论开写；随后产出改写版：`content/minimax-to-researcher-x-post-20260311.md`
- 06:30 按 Hang 判断继续去术语化：把“数字裸奔 / 框架带偏”改成更大白话的表达——**“把不确定的东西说得很确定” / “把不该它做的判断也一起做了”**
- 06:34 起号内容定稿，最终采用文件：`content/minimax-to-researcher-x-post-20260311.md`；Hang 判断这版可以收，不再继续打磨，避免把劲磨没
- 06:44 记忆 ATXP 云备份已成功：同步 238 个文件；这条链路已确认是 **ATXP 云备份**，不是 GitHub 备份
